# Contribuir

1. Crea rama desde `develop`: `feat/*`, `fix/*`.
2. Usa Conventional Commits: `feat: ...`, `fix: ...`, `docs: ...`
3. Agrega/actualiza tests y docs.
4. Abre PR a `develop` con template; espera revisión y checks verdes.

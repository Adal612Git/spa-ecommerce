import request from 'supertest';
import { describe, it } from 'vitest';

describe.skip('auth', () => {
  it('should register and login user, handle duplicates, issue JWT and protect endpoints', async () => {
    // Implement test logic with request(app)
  });
});

import request from 'supertest';
import { describe, it } from 'vitest';

describe.skip('cart', () => {
  it('should add/remove items, compute subtotal, check stock and persist cart', async () => {
    // Implement test logic
  });
});

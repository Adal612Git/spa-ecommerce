import request from 'supertest';
import { describe, it } from 'vitest';

describe.skip('orders', () => {
  it('should create order as PENDING, confirm via webhook and ensure idempotency and signature validation', async () => {
    // Implement test logic
  });
});
